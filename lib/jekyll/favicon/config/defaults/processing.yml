favicon:
  processing:
    defaults: &defaults
      background: white
      define: null
      density: null
      alpha: Transparent
      gravity: Center
      resize: 10x10
    input:
      .svg:
        <<: *defaults
        density: 2000
      .png:
        <<: *defaults
    output:
      .svg:
        <<: *defaults
        resize: null
      .ico:
        <<: *defaults
        alpha: Remove
        gravity: center
      .png:
        <<: *defaults
